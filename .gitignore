# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
.DS_Store

# Swift Package Manager
.build/
.swiftpm/

# Note: MultiPathStoryTool.xcodeproj and Generated/Info.plist are committed
# (generated by `xcodegen generate` from project.yml) so the project can be
# opened without requiring xcodegen to be installed. Re-run `xcodegen
# generate` after editing project.yml and commit the resulting changes.
